<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>This will be a form to create a new event</h1>

    <form action="/events" method="post">
        <div>
            <label for="name">Event name:</label>
            <input type="text" name="name" required placeholder="A very important meetin...">
        </div>
        <div>
            <label for="location">Location:</label>
            <select name="location" required id="location-input">
                <option value="">Add a location</option>
                <option value="in_person">In person</option>
                <option value="virtual">Virtual</option>
            </select>
        </div>
        <div id="address" style="visibility: hidden;">
            <label for="address">Address:</label>
            <input type="text"  placeholder="Negra Arroyo Lane, 368, ABQ" name="address">
        </div>
        <div>
            <label for="description">Description / Instructions: </label>
            <textarea name="description" id="" cols="30" rows="10" placeholder="Write any details your invitee should know about the event" required></textarea>
        </div>
        <div>
            <label for="slug">Link:</label>
            <p>appointr.com/<%= username %>/</p>
            <input type="text" name="slug" required placeholder="examplelink" pattern="^[a-z0-9_-]*$">
        </div>
        <p>Date range for this event:</p>
        <div>
            <label for="star-date-range">Start date:</label>
            <input type="date" value=<%= currentDate %> min=<%= currentDate %>>
        </div>
        <div>
            <label for="end-date-range">End date:</label>
            <input type="date" value=<%= currentDate %> min=<%= currentDate %>>
        </div>
        <div>
            <button>Create meetin</button>
        </div>
    </form>

    <script>
        let locationInput = document.querySelector('#location-input');
        let addressContainer = document.querySelector('#address');
 
        locationInput.addEventListener('input', () => {
            if(locationInput.value === 'in_person'){
                addressContainer.style.visibility = 'visible';
            }else{
                addressContainer.style.visibility = 'hidden';
            }
        })

    </script>
</body>
</html>